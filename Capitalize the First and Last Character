
string = "welcome to geeksforgeeks"
string = string.split(" ")
new_str = ""
for i in string:
    new = i[0].upper() + i[1:]
    new_str += new[:-1] + new[-1].upper() +" "
    
    print(new)
    
    # string[i] = i[0].upper() + string[1:]
    
print(new_str)
   
